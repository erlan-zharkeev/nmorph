import type { Meta } from '@storybook/vue3';
import { NmorphMediaGallery } from '@/components';
import { boolean, number, object, render, select, type Story } from '#storybook/story-utils';

const meta: Meta = {
  title: 'Nmorph/Data',
  parameters: {
    layout: 'centered',
  },
};

export default meta;

export const MediaGallery: Story = {
  args: {
    modelValue: false,
    design: 'nmorph',
    items: [],
    initialIndex: 0,
    activeIndex: undefined,
    zIndex: undefined,
    height: undefined,
    previewWidth: undefined,
    previewHeight: undefined,
    showTrigger: false,
    showTriggerName: true,
    showTriggerSize: true,
    showTriggerActions: true,
    showTriggerPreviewAction: true,
    showTriggerFullscreenAction: true,
    showTriggerDownloadAction: true,
    showTriggerPlayButton: true,
    triggerLayout: 'grid',
    triggerClass: undefined,
    triggerStyle: undefined,
    triggerItemClass: undefined,
    triggerItemStyle: undefined,
    triggerImageFit: 'cover',
    triggerVideoFit: 'cover',
    showNavigationButtons: true,
    showActionBar: true,
    showFileName: true,
    showFileSize: true,
    showFileActions: true,
    showPlaybackButton: true,
    imageFit: 'contain',
    videoFit: 'contain',
  },
  argTypes: {
    modelValue: boolean,
    design: select(['nmorph', 'plain']),
    items: object,
    initialIndex: number,
    activeIndex: number,
    zIndex: number,
    height: object,
    previewWidth: object,
    previewHeight: object,
    showTrigger: boolean,
    showTriggerName: boolean,
    showTriggerSize: boolean,
    showTriggerActions: boolean,
    showTriggerPreviewAction: boolean,
    showTriggerFullscreenAction: boolean,
    showTriggerDownloadAction: boolean,
    showTriggerPlayButton: boolean,
    triggerLayout: select(['grid', 'natural', 'mosaic']),
    triggerClass: object,
    triggerStyle: object,
    triggerItemClass: object,
    triggerItemStyle: object,
    triggerImageFit: select(['cover', 'contain']),
    triggerVideoFit: select(['cover', 'contain']),
    showNavigationButtons: boolean,
    showActionBar: boolean,
    showFileName: boolean,
    showFileSize: boolean,
    showFileActions: boolean,
    showPlaybackButton: boolean,
    imageFit: select(['cover', 'contain']),
    videoFit: select(['cover', 'contain']),
  },
  render: render({ NmorphMediaGallery }, '<NmorphMediaGallery v-bind="args" v-model="modelValue" />'),
};
